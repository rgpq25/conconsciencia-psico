# Project Instructions

- Use `pnpm` for package management and scripts in this Astro app.
- Prefer `pnpm install`, `pnpm dev`, `pnpm build`, and `pnpm preview`.
- Do not use `npm` or `yarn` unless explicitly requested.
